doctype html
html
  head
    title Users
    link(rel="stylesheet", href="/styles.css")
    link(rel="icon", href="/favicon.ico")
  body(class=theme)
    h1 Користувачі
    ul
      each user in users
        li
          a(href=`/users/${user.id}`) #{user.name}